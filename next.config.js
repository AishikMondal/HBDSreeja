/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/HBDSreeja',     // change to your repo name
  assetPrefix: '/HBDSreeja/',
};

module.exports = nextConfig;
